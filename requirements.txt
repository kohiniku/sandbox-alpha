yfinance>=0.2.0
pandas>=2.0.0
numpy>=1.24.0
requests>=2.28.0
matplotlib>=3.7.0
# Phase 3: expert-mode strategies (mean-variance, RL, foundation models).
# scipy/sklearn are lightweight (~200MB total) and enable a big chunk of
# published techniques (statsmodels-style, sklearn.linear_model,
# scipy.optimize for portfolio construction). Torch is deferred to a
# separate GPU image because CPU-only torch is ~250MB and torch+cuda is
# ~5GB — only enable when a real training job needs it.
scipy>=1.10.0
scikit-learn>=1.3.0
statsmodels>=0.14.0
